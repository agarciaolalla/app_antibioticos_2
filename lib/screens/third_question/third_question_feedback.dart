import 'package:flutter/material.dart';

class ThirdQuestionFeedbackScreen extends StatelessWidget {
  const ThirdQuestionFeedbackScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ThirdQuestionFeedbackScreen'),
      ),
    );
  }
}
