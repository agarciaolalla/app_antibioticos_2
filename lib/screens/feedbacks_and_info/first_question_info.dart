import 'package:flutter/material.dart';

class FirstQuestionInfoScreen extends StatelessWidget {
  const FirstQuestionInfoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('FirstQuestionInfoScreen'),
      ),
    );
  }
}
